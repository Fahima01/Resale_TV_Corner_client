import React from 'react';

const Dashboard = () => {
    return (
        <div>
            <button className='btn btn-warning'>hello world</button>
        </div>
    );
};

export default Dashboard;